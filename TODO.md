# TODO - Terra App Improvements

## Completed

1. ~~**Add hover underline to footer links**~~ — Added `text-decoration: underline` to `.footerLink:hover` in `Layout.module.css` to make the Privacy/Terms links visually interactive. ✓

## Pending

2. **Add a theme-color meta tag** — Add a single `<meta>` tag in `entry-server.tsx` so mobile browsers tint their chrome to match Terra's warm-cream background.

3. **Add scale transform on hero CTA button hover** — Add a small `transform: scale(1.03)` in `index.module.css` for tactile hover feedback on the home page buttons.

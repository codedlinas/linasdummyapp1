# TODO - Small Improvements for Terra App

## Completed

- [x] **Add a meta description tag** in `src/entry-server.tsx` — improves SEO and link previews.

## Pending

- [ ] **Replace `<a>` with `<A>` in the Features CTA** in `src/routes/features.tsx` — enables client-side navigation instead of full-page reloads.

- [ ] **Add hover underline to footer links** in `src/components/Layout.module.css` — gives users a clearer visual cue that the links are clickable.
